<!DOCTYPE html>
<html lang="en">
<head>

  <title>Weather App</title>
  <link rel="stylesheet" href="css/main.css" />
</head>
<body>
<h2>{{>header}}</h2>
  <div class="app-wrap">
    <header>
    </header>
    <main>
      <section class="location">
        <div class="city">{{location}}</div>
                <div class="lonlat">longitude {{lon}}</div>
                <div class="lonlat">latitude {{lat}}</div>


      </section>
      <div class="current">
        <div class="temp">{{temp}}<span>°c</span></div>
        <div class="weather"> it’s {{condition}} <img src="{{icon}}" alt=""> </div>
      </div>
    </main>
  </div>
</body>
</html>